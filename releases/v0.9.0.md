# EdgeForge 0.9.0 发布记录

- 开发日期：2026-08-18
- 状态：代码与契约验证通过；真实 IREE binary 验证待设备条件满足
- 阶段：V9 IREE `conv_nchwc` runtime-only Pipeline

## 实现与安全修复

- packed rank-9 `conv_nchwc` Operator IR，支持 stride、dilation 和布尔 `accumulate`。
- 受信任 IREE ukernel 的 prebuilt `compile → correctness → benchmark` Pipeline，保存 manifest Artifact、source identity、throughput 和 timings。
- test/benchmark command 经过 Worker allow-list 与 work-root/workdir 边界，执行使用 `shell=False`。
- 非 reference backend 禁止从 `operator_benchmark` 静默落回 Python reference；IREE 必须使用 `kernel_pipeline`。

## 自动化验证

- 全量测试：57/57 通过。
- 新增覆盖：IREE flag 转发、throughput parsing、trusted metadata、allow-list、`conv_nchwc` accumulate 类型和错误 backend 路径。
- `compileall` 与 `git diff --check`：通过。
- 日志：`logs/archive/v0.9.0/development/test-followup-targeted.log`、`logs/archive/v0.9.0/development/test-followup-full.log`。

## 真实设备边界

当前工作区没有 `iree-build-runtime/runtime/src/iree/builtins/ukernel/tools/conv_nchwc_test` 或 `conv_nchwc_benchmark` 可执行文件，因此本次没有声称真实 IREE x86_64 或 Orange Pi correctness/benchmark。fake executable 只验证 EdgeForge 适配器契约，不能作为硬件性能证据。

下一步将把实际预构建 binary 放入 Worker work-root，先在 x86_64 验证，再用 ARM64-native 或正确交叉编译 binary 在 Orange Pi 重新注册独立 Kernel ID；两者结果不得互相复用。
