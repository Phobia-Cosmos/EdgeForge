# EdgeForge 0.9.0 发布记录

- 开发日期：2026-08-18
- 状态：代码与 x86_64/ARM64 双节点契约验证通过；真实 IREE binary 验证被开放的 upstream #24760 阻塞
- 阶段：V9 IREE `conv_nchwc` runtime-only Pipeline

## 实现与安全修复

- packed rank-9 `conv_nchwc` Operator IR，支持 stride、dilation 和布尔 `accumulate`。
- 受信任 IREE ukernel 的 prebuilt `compile → correctness → benchmark` Pipeline，保存 manifest Artifact、source identity、throughput 和 timings。
- test/benchmark command 经过 Worker allow-list 与 work-root/workdir 边界，执行使用 `shell=False`。
- 非 reference backend 禁止从 `operator_benchmark` 静默落回 Python reference；IREE 必须使用 `kernel_pipeline`。
- `blocked-*` validation status 在 executable 解析前被拒绝；真实执行必须绑定 repository、40 位 commit 和 64 位 patch SHA-256。只有显式的 `contract-only-not-real-iree` 可绕过 source identity，用于假 executable 合同测试。

## 自动化验证

- 全量测试：59/59 通过。
- 新增覆盖：IREE flag 转发、throughput parsing、trusted metadata、allow-list、blocked upstream 状态、无效 source identity、`conv_nchwc` accumulate 类型和错误 backend 路径。
- `compileall` 与 `git diff --check`：通过。
- 日志：`logs/archive/v0.9.0/development/test-source-identity-targeted-20260819.log`、`logs/archive/v0.9.0/development/test-pr-final-20260819.log`、`logs/archive/v0.9.0/development/verify-source-identity-final-20260819.log`、`logs/archive/v0.9.0/development/upstream-source-audit-20260819.md`。

## 真实设备边界

IREE #24760 是开放 issue，不是已完成 PR；此前错误关联的 commit `a82330a…` 实际属于 IREE #24800，已从配置移除。当前工作区与 Orange Pi 都没有匹配 #24760 的 `conv_nchwc_test` 或 `conv_nchwc_benchmark` 可执行文件，因此没有声称真实 IREE x86_64 或 Orange Pi correctness/benchmark。

本机 x86_64 与 Orange Pi aarch64 已分别通过真实 EdgeForge control plane 和 Worker 完成 `contract-only-not-real-iree` 验证，任务、Pipeline event、两条 Benchmark 和两个 manifest Artifact 均已持久化，且确认 `dilation_h=2`、`dilation_w=3`、`accumulate=false` 被转发。0.025 ms 与 0.08 ms 是 fake executable 写死的协议值，不能作为 IREE 或硬件性能证据。

干净的双节点报告保存在 `logs/archive/v0.9.0/contract-validation-source-gate-final-20260819/report.json`，独立 SQLite 保存在 `data/edgeforge-v0.9.0-contract-validation-source-gate-final-20260819.db`，两个 compiler manifest 保存在 `data/edgeforge-v0.9.0-contract-artifacts-source-gate-final-20260819/`。

下一步先跟踪或实现 IREE #24760 所需的 `CPULowerToUKernels.cpp`、ukernel ABI/params 与 tile addressing dilation plumbing，取得可审查的 commit/patch 后再生成实际预构建 binary。随后先在 x86_64 验证，再用 ARM64-native 或正确交叉编译 binary 在 Orange Pi 重新注册独立 Kernel ID；两者结果不得互相复用。Vulkan 与本 CPU ukernel PR 无关，Meles 和 P550 不参与本次验收。
