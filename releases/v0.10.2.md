# EdgeForge 0.10.2 发布验证

- 日期：2026-08-20
- 范围：Backend/Target capability contract 与上游贡献准备
- 版本：`0.10.2`

## 自动化验证

执行：`PYTHONPATH=src python3 -m unittest discover -s tests -q`

结果：`72/72` 通过。

覆盖：Backend Registry 列举、IREE architecture/device 必填、RKNN ARM64/NPU 约束、custom backend target、Worker advertised backends、模型任务 backend/target 校验，以及既有模型/实验/LoP/Artifact 回归。

## 上游边界

本版本只在 EdgeForge 内实现 explicit Backend/Target contract，不 clone 或修改 IREE 源码，不实现 IREE #24760。后续如果真实 BrainUICL 模型在 IREE/ONNX Runtime target 上产生可复现错误，再从 Artifact 导出最小 reproducer，建立独立上游分支和测试。

## 日志

本版本进程 JSONL、失败/成功任务、SQLite 和 Artifact 按 `logs/archive/v0.10.2/` 归档；不得覆盖 V0.10.1 的 LoP 验证记录。
