# EdgeForge 0.10.1 发布验证

- 日期：2026-08-19
- 范围：BrainUICL/RA-EEG 历史结果迁移与 Backend/Target 文档
- 版本：`0.10.1`

## 自动化验证

执行：`PYTHONPATH=src python3 -m unittest discover -s tests -q`

结果：`66/66` 通过。

## 迁移验证

执行 `scripts/build-raeeg-catalog.py` 扫描 `/home/undefined/Desktop/bci/code/tta_security/BrainUICL`，发现 817 个 `metrics.json/RESULTS.json`，生成约 1.2 MiB catalog。迁移只保存结果文件 digest、相对路径和规范化 ExperimentSpec，不复制原始 EEG、checkpoint 或源码。

分组统计：`aligned-full49=160`、`method-transfer=491`、`historical-unclassified=166`。这些结果只是历史归档；在 protocol、dataset manifest 和 comparison group 审核完成前，不允许直接作为 Capability Gate 或模型性能结论。

Worker capability 另外增加 Backend advertisement；默认只有 `python-reference`，其余 Backend 必须由节点显式通过 `EDGEFORGE_BACKENDS` 声明并经过真实验证。

## LoP Pipeline Validation

在迁移 8 组 curated ISRUC/BrainUICL 结果后，使用 `config/raeeg-lop-edgeforge-v0.10.1.json` 由 `worker-4070s-lop` 重放 stage 0/10/25 的 LoP smoke。任务 `b3598e30f1db4ea3b22f4527f6e400cd` 成功，运行时版本为 `0.10.1`，Experiment Bundle digest 为 `24ff34db216123baeff947cadd86fd989201bedb870dc64e86f34c97116d991a`，source result digest 为 `6314253d4076854aa5e42ea45b70786e9922595acabbe3475426cd27e675c7c4`。

观测值：stage 0/10/25 的 plasticity accuracy gain 为 `+0.05 / -0.05 / 0.00`，均值为 `0.00`；Transformer effective-rank 与 future plasticity 的 smoke Pearson 为 `0.5704744175`。该运行只有 3 个 stage、每阶段最多 4 个 sequence、1 个 seed，且使用 supervised-oracle fixed-budget probe，因此只能证明 EdgeForge 的 LoP 数据、指标、Artifact 和日志链路可用，不能证明或否定 LoP 机制。

持久归档位于 `logs/archive/v0.10.1/lop-validation-20260819/`，包含控制面/Worker JSONL、SQLite、WAL checkpoint 后的 `edgeforge-backup.db`、9 个 Artifact 和任务回执。

## 运行边界

要执行历史导入，Worker 必须将 BrainUICL 仓库设置为 `--work-root`，并配置受信任的 BrainUICL Python 环境。若 Worker 离线，任务保持 queued；不会把本地文件路径误当成中央 Artifact，也不会伪造编译或开发板性能。
