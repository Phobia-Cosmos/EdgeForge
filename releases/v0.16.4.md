# EdgeForge 0.16.4 多 seed EEG 预训练与后端验证

- 日期：2026-08-26
- 状态：development snapshot；预训练和主机编译链路通过，正式 LoP 结论仍需重新运行多 seed CL trajectory

## 预训练

ISRUC 使用 canonical processed root `/home/undefined/Disk/datasets/brainuicl/processed/isruc_group1_npy_float32`，输入 `(20,8,3000)`；FACED 使用 `/home/undefined/Disk/datasets/FACED_processed`，输入 `(20,32,2500)`。两个数据集都固定 `split_seed=4321`，并独立训练真实 seed `4321/4322/4323`，每个 seed 100 epoch、独立初始化、独立 optimizer 和独立 checkpoint 目录。

| dataset | seed 4321 | seed 4322 | seed 4323 |
| --- | ---: | ---: | ---: |
| ISRUC best val ACC | 0.643916 | 0.651141 | 0.646578 |
| ISRUC best val macro-F1 | 0.631231 | 0.630521 | 0.585920 |
| FACED best val ACC | 0.304688 | 0.300781 | 0.300000 |
| FACED best val macro-F1 | 0.295959 | 0.285433 | 0.263080 |

产物位于 `/home/undefined/Disk/ai-storage/EdgeForge/raeeg-pretrain/v0.16.4/`，包含 `environment.json`、每个 seed 的 `command.txt`、`train.log`、`split.json`、`pretrain_metrics.json`、checkpoint manifest 和 SHA-256。原始数据与外部 BrainUICL 源码未修改。

## 主机后端

- ISRUC 三个 seed：`torch.export` graph digest 均为 `7fc77e44d6f5b6bc3094d876c670c423de05939cf376f821eea84cbbc76e5d2b`；CPU eager correctness 全部通过；CUDA Inductor（`inductor-no-pattern`）全部通过，最大绝对误差 `1.76e-5`、`2.19e-5`、`2.93e-5`。
- ISRUC CUDA Inductor 稳态延迟约 `0.855–0.865 ms`/sample window；CPU eager 约 `5.77–8.79 ms`。
- FACED seed 4321：export graph node count 236、输出 `(1,9,20)`；CPU eager correctness 通过，CUDA Inductor correctness 通过，最大绝对误差 `6.78e-4`，稳态延迟约 `0.675 ms`。

主机 artifact 位于 `.edgeforge/v0.16.4-brainuicl-backends/`。`brainuicl.pt2` 是 torch.export/Graph IR artifact，不能直接作为 RKNN、Vulkan、OpenCL 或 IREE 模型文件广播。

## RK3588 运行时证据

Orange Pi smoke 文件为 `.edgeforge/rk3588-accelerator-smoke-v0.16.4.json`，结果为 `pass`：Mali-G610 OpenCL vector-add max error `0.0`；RKNN C API MobileNet zero-input 三次输出 digest 一致；显式 g24p0 Vulkan ICD 完成 loader → instance → physical-device enumeration。`scientific_conclusion_allowed=false`，这不是 BrainUICL EEG 模型级部署结论。

## LoP 解释与后续

0.16.3 的单 seed、小预算矩阵仍不能说明模型稳定或不会发生 LoP。0.16.4 解决的是正式多 seed pretrain 缺失问题；它还不是 LoP 证据本身。下一阶段应在两个数据集、三个 seed 上固定同一 subject order、stage grid、probe budget、fresh control 和 retention set，重新生成 CL trajectory，再进行 seed-cluster bootstrap 和遗忘/LoP 审计。

详细运行日志保存在 `logs/archive/v0.16.4/`；大 checkpoint、原始 EEG 与派生数据不提交 Git。
